import { FxError, Result, Void } from "@microsoft/teamsfx-api";
import { DriverContext } from "../interface/commonArgs";
import { ExecutionResult, StepDriver } from "../interface/stepDriver";
import { GenerateEnvArgs } from "./interface/generateEnvArgs";
export declare class CreateOrUpdateEnvironmentFileDriver implements StepDriver {
    description: string;
    readonly progressTitle: string;
    run(args: GenerateEnvArgs, context: DriverContext): Promise<Result<Map<string, string>, FxError>>;
    execute(args: GenerateEnvArgs, ctx: DriverContext): Promise<ExecutionResult>;
    private handler;
    /**
     * Pop up input text to input OpenAI environment variables, or return UserCancel error.
     * @param ctx
     * @param args The arguments passed to the driver.
     * @param envOutput Used to store the resolved environment variables, which will be written to the environment file.
     * @returns
     */
    askForOpenAIEnvironmentVariables(ctx: DriverContext, args: GenerateEnvArgs, envOutput: Map<string, string>): Promise<Result<Void, FxError>>;
    private processOpenAIEnvironmentVariable;
    private validateArgs;
}
//# sourceMappingURL=createOrUpdateEnvironmentFile.d.ts.map