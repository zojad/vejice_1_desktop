import { MaturityLevel, DependencyType } from "./types";
export function getLogEntriesForLevel(logEntries, ...levels) {
    return logEntries.filter((entry) => levels.indexOf(entry.level) !== -1);
}
export function maturityLevelToString(level) {
    switch (level) {
        case MaturityLevel.experimental:
            return "experimental";
        case MaturityLevel.preview:
            return "preview";
        case MaturityLevel.stable:
            return "stable";
        default:
            throw new Error("unknown level");
    }
}
export function dependencyTypeToString(type) {
    switch (type) {
        case DependencyType.abstractions:
            return "abstractions";
        case DependencyType.serialization:
            return "serialization";
        case DependencyType.authentication:
            return "authentication";
        case DependencyType.http:
            return "http";
        case DependencyType.bundle:
            return "bundle";
        case DependencyType.additional:
            return "additional";
        default:
            throw new Error("unknown type");
    }
}
export function checkForSuccess(results) {
    for (const result of results) {
        if (result && result.message) {
            if (result.message.includes("Generation completed successfully")) {
                return true;
            }
        }
    }
    return false;
}
//# sourceMappingURL=utils.js.map